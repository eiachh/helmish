apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.name }}-configmap
data:
  before: {{ .Values.beforeText }}
  {{ if or .Values.trueCond1 .Values.trueCond2 }}
  case1: bothTrue
  {{ end }}
  {{ if or .Values.trueCond1 .Values.falseCond1 }}
  case2: trueOrFalse
  {{ end }}
  {{ if or .Values.falseCond1 .Values.falseCond2 }}
  case3: bothFalse
  {{ end }}
  after: {{ .Values.afterText }}